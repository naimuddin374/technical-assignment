(function ($) {
    'use strict';
    // nice select
    var niceSelect = $('select');
    if (niceSelect.length) {
        niceSelect.niceSelect();
    }

    // active video popup
    // $("a.bla-1").YouTubePopUp();

})(jQuery);